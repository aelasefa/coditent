import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Company profile — Coditent",
};

export default function CandidateCompanyProfileLayout({ children }: { children: React.ReactNode }) {
  return children;
}
